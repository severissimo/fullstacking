const add = (x, y) => x + y;
const sub = (x, y) => x - y;
const div = (x, y) => x / y;
const mul = (x, y) => x * y;
const square = (x) => x * x;
const PI = 3.14159;

const math ={
    add: add,
    sub: sub,
    div: div,
    mul: mul,
    square: square,
    PI: PI
}

module.exports = math;